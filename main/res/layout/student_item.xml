<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginBottom="12dp">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/studentImageView"
        android:layout_width="wrap_content"
        android:layout_height="64dp"
        android:maxWidth="100dp"
        android:adjustViewBounds="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/studentFullNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/studentImageView"
        app:layout_constraintTop_toTopOf="@+id/studentImageView"
        android:layout_marginStart="24dp"
        android:textColor="@color/black"
        tools:text="@tools:sample/full_names"
        />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/studentPrivateNumberTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        tools:text="@tools:sample/us_phones"
        app:layout_constraintStart_toStartOf="@+id/studentFullNameTextView"
        app:layout_constraintTop_toBottomOf="@+id/studentFullNameTextView" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/studentEmailTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@tools:sample/lorem"
        app:layout_constraintStart_toStartOf="@+id/studentPrivateNumberTextView"
        android:layout_marginTop="6dp"
        app:layout_constraintTop_toBottomOf="@+id/studentPrivateNumberTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>
